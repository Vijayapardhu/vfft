/** Tailwind CSS v4 uses the dedicated PostCSS plugin. No tailwind.config.js — theme is configured CSS-first in src/app/globals.css via @theme. */
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
